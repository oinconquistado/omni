{
  "name": "@repo/server-core",
  "version": "0.0.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "lint": "biome lint .",
    "check-types": "tsc --noEmit",
    "test": "echo \"No tests configured for server-core package\" && exit 0"
  },
  "dependencies": {
    "@repo/shared-types": "workspace:*",
    "fastify": "^5.2.0",
    "@fastify/swagger": "^9.3.0",
    "@fastify/swagger-ui": "^5.0.1",
    "@fastify/request-context": "^6.0.1",
    "@sentry/node": "^8.46.0",
    "pino": "^9.9.1",
    "pino-pretty": "^13.1.0"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "typescript": "^5.7.2"
  }
}
